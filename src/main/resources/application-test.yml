spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cat_food_tracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: catfoodapp
    password: catfoodapp
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect